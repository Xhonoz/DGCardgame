export type IconName = "arrow-left" | "arrow-right";

export type ComponentProps = {
  children?: React.ReactNode;
  className?: string;
};

export type card = {
  name: string;
  description: string;
  image: string;
  type: string;
};
